# ESP-IDF
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Python
__pycache__/

# CMake
cmake-build-*/
CMakeCache.txt
CMakeFiles/

# Editors
.claude/
.vscode/
.idea/
*.swp
*~
.cache/
compile_commands.json

# OS
.DS_Store

# Vendor schematic: proprietary, kept locally only
docs/vendor/*.pdf

# Audio assets: copyrighted recordings, kept local. Reproduce with the app's
# tools/fetch_audio.sh. The repo ships the fetch script, never the audio.
*.mp3
*.pcm
*.wav
audio_fs/

# Secrets: WiFi/API credentials stay local, never committed. The .env is created
# for the user to fill; wifi_credentials.h is generated from it at build time.
.env
wifi_credentials.h
